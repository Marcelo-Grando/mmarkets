import moment from "moment";

export const year = moment().format("YYYY");

export const month = moment().format("MMM");

export const date = moment().format("YYYY-MM-DD");

export const time = moment().format("HH:mm:ss");

export const hour = moment().format("HH");
